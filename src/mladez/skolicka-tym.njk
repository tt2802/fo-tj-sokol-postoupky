---
title: Tým Školičky
permalink: /mladez/skolicka/tym/index.html
layout: layouts/page.njk
---


<section class="section">
  <div class="container">
    <h1>Tým Školičky (U8)</h1>
    <p class="muted">Soupiska hráčů kategorie Školička.</p>
  </div>
</section>

<section class="section">
  <div class="container">
    <h2>Hráči</h2>

    {% set skolickaPlayers = players.youth["skolicka"] | default([]) %}

    {% if skolickaPlayers | length == 0 %}
      <p class="muted">Zatím zde nejsou žádní hráči. Doplníš je v administraci v sekci „Data → Hráči – Mládež - Školička".</p>
    {% else %}
      <div class="grid cards" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 2rem;">
        {% for p in skolickaPlayers %}
          <article class="card">
            <div class="card__media" style="--media-aspect: 1 / 1; --media-radius: 4px 4px 0 0;">
              {% if p.photo %}
                {% image p.photo, "Fotka hráče " ~ p.name, "(max-width: 280px) 100vw, 280px", "player-photo" %}
              {% else %}
                <div class="media-frame__placeholder media-frame__placeholder--compact">Bez fotky</div>
              {% endif %}
            </div>

            <div class="card__body">
              <h3 class="card__title">
                {% if p.number %}<span style="font-size: 0.8em; color: var(--text-muted);">#{{ p.number }}</span> {% endif %}{{ p.name }}
              </h3>
              {% if p.position %}
                <p class="muted">{{ p.position }}</p>
              {% endif %}
            </div>
          </article>
        {% endfor %}
      </div>
    {% endif %}
  </div>
</section>
